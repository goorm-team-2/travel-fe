import Auth from './Auth';

export default function AuthPage() {
  return (
    <div className="bg-background min-h-[100vh] w-full flex items-center justify-center p-0 m-0">
      <Auth />
    </div>
  );
}
